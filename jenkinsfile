pipeline {
    agent any
    tools {
        maven 'apache-maven-3.0.1' 
    }
    stages {
        stage('KKK Example') {
            steps {
                sh 'mvn --version'
                echo "hello this is your job"
            }
        }
    }
}
